apiVersion: v1
kind: Service
metadata:
  name: {{ include "worldos.fullname" . }}-material-service
spec:
  selector:
    app: material-service
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
  type: ClusterIP