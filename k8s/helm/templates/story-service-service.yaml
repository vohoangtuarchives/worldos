apiVersion: v1
kind: Service
metadata:
  name: {{ include "worldos.fullname" . }}-story-service
spec:
  selector:
    app: story-service
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
  type: ClusterIP